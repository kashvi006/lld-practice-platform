{
  "name": "lld-practice-platform",
  "version": "1.0.0",
  "description": "LLD Practice Platform with explainable evaluation for CipherSchools assignment",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix backend run db:push && npm --prefix backend run db:seed && npm --prefix frontend install && npm --prefix frontend run build",
    "test": "npm --prefix backend run test",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build"
  }
}